<Window x:Class="DumpViewer.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DumpViewer" Height="520" Width="860">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header / Actions -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
        <Button Content="Load dump" Width="120" Click="LoadDump_Click" />
            <TextBlock Margin="12,0,0,0"
                       VerticalAlignment="Center"
                       Text="{Binding StatusMessage}" />
        </StackPanel>

        <!-- Key fields -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="ProductId:" FontWeight="SemiBold" />
            <TextBlock Grid.Column="1" Text="{Binding ProductId}" Margin="6,0,18,0"/>

            <TextBlock Grid.Column="2" Text="RunTime:" FontWeight="SemiBold" />
            <TextBlock Grid.Column="3" Text="{Binding RunTime}" Margin="6,0,18,0"/>

            <TextBlock Grid.Column="4" Text="Price:" FontWeight="SemiBold" />
            <TextBlock Grid.Column="5" Text="{Binding Price}" Margin="6,0,0,0"/>
        </Grid>

        <!-- Greeks table -->
        <GroupBox Grid.Row="2" Header="Greeks">
            <DataGrid ItemsSource="{Binding Greeks}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Key}" Width="*"/>
                    <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <!-- Footer -->
        <TextBlock Grid.Row="3"
                   Margin="0,10,0,0"
                   Opacity="0.7"
                   Text="WPF + MVVM + async + tests (mini IT-Quant dump viewer)" />
    </Grid>
</Window>
