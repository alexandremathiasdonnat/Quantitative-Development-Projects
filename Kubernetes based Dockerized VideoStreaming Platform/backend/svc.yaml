apiVersion: v1
kind: Service
metadata: { name: ginflix-backend, namespace: ginflix }
spec:
  type: NodePort
  selector: { app: ginflix-backend }
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30081